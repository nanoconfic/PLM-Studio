# 从这里开始

直接告诉 Agent 你要做什么：

```text
根据需求做一个原型
把已有 HTML 嵌入 PLM
先做原型，验收后嵌入
```

需要查看机器可读的工作区入口状态时运行：

```powershell
powershell -NoProfile -File .\controller\run.ps1 -Json
```

Controller 列出三个能力模式和 Active 扩展。选定扩展后，Agent 按当前阶段的缺失清单补齐需求；每个能力都有独立的开工门禁和验收。

原型只交付静态页面；嵌入只接收已有页面；联动模式在原型验收后再进入原系统嵌入。需求确认和对应开工检查通过前，不进入实现。

扩展始终是长期可迭代的容器。完成只表示当前迭代已经验收，不会让扩展退出可选列表。
