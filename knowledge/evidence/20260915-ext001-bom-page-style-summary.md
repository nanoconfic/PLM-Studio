# EXT-001 运行时页面与样式证据摘要（已脱敏）

- **证据 ID**：`EVID-20260915-EXT001-BOM-STYLE`
- **状态**：Candidate
- **采集时间**：2026-09-15（DEV）
- **环境快照**：`knowledge/environment-snapshots/20260915-154808-643-499c63.yaml`
- **来源**：用户提供的 DEV Web 地址；浏览器自动化以人工授权的已配置账号登录后采集。
- **来源指纹**：运行时页面，无源码清单；对应需求文档 SHA-256：`bd2bfd0611e6f232030cdde1e97d014411243c927f235ba796bf4296cf4387a0`。
- **脱敏说明**：未保留截图、完整 DOM、Cookie、登录态、账号、密码、业务对象/检出清单或接口请求。以下内容仅保留页面结构与计算样式摘要。

## 可复现步骤

1. 打开已配置的 DEV Web 地址并登录。
2. 在左侧“导航菜单”点击“产品数据管理”。
3. 在展开项中点击“BOM管理”。
4. 不选择、编辑、检出、保存或提交任何业务对象；观察已打开的 BOM 管理工作区。

## 运行时观察

- 登录页标题为 `ECM Platform`，可见主题为“蓝色”。
- 主应用采用左侧 Accordion 导航；“产品数据管理”下包含“BOM管理”。
- BOM 管理打开后以工作区页签/面板呈现；可见操作按“浏览操作、流程操作、其他操作、切换面板、检查比较、汇总下载、ERP集成、结构管理器”分组。
- “浏览操作”中已存在刷新、加载根节点、文档显示/隐藏、切换有效性、切换视图、保存基线、加载基线、浏览图文档、全部展开、展开到等动作。
- 内容区采用树/网格型 BOM 工作区；本次采集时数据区为“暂无数据”，未进入具体 BOM。
- 可见网格字段包括 BOM行、图文档、工艺卡片、工艺过程、下发ERP、位置、数量、是否借用、位号、品目、生效日期等。

## 样式 token 摘要（运行时计算值）

| 元素 | 观察值 | 适用建议 |
|---|---|---|
| 基础字体 | `微软雅黑, "Lucida Grande", "Lucida Sans", Arial, sans-serif` | 原型沿用该字体栈。 |
| 基础字号/文本 | 12px；`rgb(51, 51, 51)` | 页面正文、导航、网格信息使用紧凑 12px。 |
| 分隔/拖拽器 | `rgb(225, 225, 225)` | 面板分隔、工具栏/网格边界采用浅灰细线。 |
| 高亮操作按钮 | `rgb(21, 126, 198)` 背景、白字、14px 加粗、1px 灰边、4px 圆角、高约 25px | “转量产”演示入口以同类紧凑蓝色按钮表达。 |
| 布局 | 左导航 + 可拖拽分栏 + 页签/面板 + 工具栏分组 + 网格 | 原型保持经典桌面 PDM 工作台信息密度，避免现代卡片化仪表盘风格。 |

## 限制与待验证

- 只在 DEV、蓝色主题、当前浏览器尺寸和无数据 BOM 工作区下验证。
- 未验证具体 BOM 打开后的工具栏 DOM、右键菜单、按钮权限状态、不同主题、不同分辨率或移动端行为。
- 未同步源码，故无法将观察结果关联到具体模板、JS/CSS 或 Command/Action 源文件。
- 此记录不得作为跨 workspace 的 Verified 结论；需要在目标环境重新验证。
