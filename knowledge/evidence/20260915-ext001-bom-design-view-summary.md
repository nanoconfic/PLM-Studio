# EXT-001 指定 BOM Design 视图运行时证据摘要（已脱敏）

- **证据 ID**：`EVID-20260915-EXT001-BOM-DESIGN-VIEW`
- **状态**：Candidate
- **采集时间**：2026-09-15（DEV）
- **环境快照**：以 `EXT-001` 最新分析会话为准。
- **来源**：用户指定的唯一查询路径；运行时浏览器只读操作。
- **脱敏说明**：未保留截图、完整 DOM、Cookie、登录态、账号、密码、创建/修改人、时间戳或网络请求。

## 可复现步骤

1. 登录 DEV。
2. 产品数据管理 → BOM管理 → 加载根节点。
3. 在“编号”条件输入用户指定的 `12100-2CB -A401` 并查询。
4. 查询返回唯一记录：气缸体总成，版本 A.001，试制。
5. 加载根节点；初始打开为 Process1 视图。
6. 使用“切换视图”后，标题为：`结构管理器：气缸体总成/12100-2CB -A401/A.001(Design)(最新版本)`。

## 运行时观察

- Design 视图标题满足用户指定的完整文本。
- 结构区显示根节点“气缸体总成/12100-2CB -A401/A.001(Design)”及 2 个一级子件，共 3 行。
- 顶部菜单栏包含“浏览操作、流程操作、其他操作、切换面板、检查比较、汇总下载、ERP集成、结构管理器”。
- 已观察到“其他操作”组的既有项目：对象授权、批量授权、发送给其他用户、消息订阅、收藏。
- 用户指定扩展入口：`其他操作 → 试制转量产`。原型已按该位置展示；原厂菜单配置、Command/Action、权限和源码实现尚未确认。

## 限制

- 只在 DEV、蓝色主题、当前角色、该唯一对象与 Design 视图下验证。
- 同一无界面自动化中的“加载根节点”确认窗口存在不稳定遮罩行为；标题与结构数据来自已成功加载并切换视图的会话。该 UI 行为不影响已验证标题结论，但后续如做自动化回归需使用可见浏览器或稳定会话方案。
- 本记录为 Candidate，不得用于其他 profile、主题、用户角色、产品版本或跨 workspace 的 Verified 结论。
